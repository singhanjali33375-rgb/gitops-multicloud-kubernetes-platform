apiVersion: v1

kind: ConfigMap


metadata:

  name: {{ include "gitops-app.fullname" . }}-config


  labels:

    {{- include "gitops-app.labels" . | nindent 4 }}



data:

  ENVIRONMENT: {{ .Values.env.ENVIRONMENT | quote }}


  APP_NAME: {{ .Values.env.APP_NAME | quote }}
